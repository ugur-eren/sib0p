import { StyleSheet } from 'react-native'

export default StyleSheet.create({
	container: {
        opacity: 0.5,
        flexDirection: 'row',
        alignItems: 'center',
		position: 'absolute',
		width: '100%',
		top: 0,
		left: 0,
        elevation: 0,
	},
})
